# Copy to .env and fill in. NEVER commit .env.

# Required for forecasting calls
ANTHROPIC_API_KEY=

# Required for --submit (create bot account first; see README)
METACULUS_TOKEN=

# Optional: point at a shared .env to source keys from (else use this file / env)
# SHARED_ENV_PATH=

# Optional: AskNews tournament credits (slot-in; keyless fallbacks used otherwise)
ASKNEWS_CLIENT_ID=
ASKNEWS_SECRET=

# Optional overrides
# TOURNAMENT_ID=summer-futureeval-2026
# ANTHROPIC_MODEL=claude-opus-4-6
# ENSEMBLE_SIZE=5
# BINARY_FLOOR=0.015
